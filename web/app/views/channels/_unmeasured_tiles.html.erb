<% channels.each do |channel_id| %>
  <%= link_to channel_path(channel_id), class: "tile" do %>
    <span class="tile-t mono"><%= channel_id %></span>
    <span class="tile-s">Mnemosyne has no data for this channel yet</span>
  <% end %>
<% end %>
